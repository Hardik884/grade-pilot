# Grade Change Intelligence - Python 3.11+
# Install:  python -m venv .venv && .venv/Scripts/python.exe -m pip install -r requirements.txt

# Core numerics / data
numpy==2.5.1
pandas==3.0.5
pyarrow==24.0.0
scipy==1.18.0

# Modelling
scikit-learn==1.9.0
statsmodels==0.14.6

# Dashboard - static HTML/CSS/JS over a thin Flask API, Chart.js from CDN with a
# vendored fallback in src/ui/static/vendor. No build step, no node.
flask==3.1.2

# Test
pytest==9.1.1
